# StorytellerPi Dependencies - Production Ready
# Core Web Framework
flask==2.3.3
flask-socketio==5.3.4

# Google Cloud Services
google-cloud-speech==2.21.0
google-cloud-texttospeech==2.14.1
google-generativeai==0.3.0

# AI/ML Services
openai==1.3.0
elevenlabs==0.2.24

# Wake Word Detection
pvporcupine==3.0.0
openwakeword==0.5.1

# Audio Processing
pyaudio==0.2.11
pygame==2.5.2
numpy==1.24.3
scipy==1.10.1
librosa==0.10.1
soundfile==0.12.1
webrtcvad==2.0.10
pydub==0.25.1

# System Utilities
python-dotenv==1.0.0
psutil==5.9.5
requests==2.31.0

# NOTE: The following are built-in Python modules and should NOT be installed:
# asyncio, threading, logging, pathlib, enum34, signal, gc, time, os, sys
# json, subprocess, re, collections, functools, typing, dataclasses
# contextlib, tempfile, shutil, socket, urllib, wave, multiprocessing
# queue, pickle, base64, hashlib, hmac, secrets, uuid, datetime, calendar
# locale, gettext, unicodedata